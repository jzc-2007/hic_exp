# Testing

Use:

```bash
bash scripts/doctor.sh
bash scripts/run_tests.sh
```

The test suite covers database initialization, message persistence, wake
requests, scheduler clamping, roster loading, directory creation, mock runner,
web routes, CLI smoke checks, and start/stop dry runs.
