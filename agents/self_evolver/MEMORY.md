# MEMORY

- HIC prompts/protocol must keep TPU safety red lines visible to any agent that can run or manage tasks.
- All agents should prefer the simplest workable path and convert recurring workflows into one-click helpers.
- Durable self-evolution memory should stay structured and size-bounded; use `python3 scripts/hicctl.py compact-notes` as the standard compaction path.
